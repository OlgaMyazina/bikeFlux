/*import * as allReducerInterface from './reducer';
function reducerCombaine(...redusers: string[]): allReducerInterface {
  let allReduce: allReducerInterface = {
    tileEvents: ''
  };
  redusers.forEach(reducer => (allReduce.tileEvents = reducer));
  console.log(`reducerCombaine`);
  return allReduce;
}
*/
